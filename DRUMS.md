# Hibiki Drum Machine 🥁

**完全無料**・広告なし・アカウント不要の、合成方式ドラムマシン・アプリです。
録音済みのドラム音源（サンプル）を一切使わず、**14種類の打楽器すべてをその場で計算**して
鳴らしています。アプリ本体は約 22 KB（gzip 圧縮時）で、追加ダウンロードはありません。

このリポジトリのシンセ（Akatsuki Synth）・ピアノ（Aozora Grand Piano）とは
独立したアプリで、`/drums/` で公開されます。

- 開発サーバー … `npm run drums:dev` → <http://localhost:5174/drums/>
- 本番ビルド … `npm run build` → `dist/drums/`

---

## なぜ「サンプルを使わない」のか

市販のドラム音源は、実機のドラムマシンや生ドラムを録音したライブラリ（数百 MB〜数 GB）を
再生しています。高品質なサンプルライブラリは**有償ライセンス**で、しかも再配布が禁止されて
いることがほとんどです。本アプリは音を合成しているため、

- **ライセンス費用ゼロ** … 第三者の録音物・IR・ライブラリを一切含みません。そのまま販売もできます
- **配信コストゼロ** … 数百 KB の静的ファイルのみ。CDN 費用も課金も不要
- **オフライン動作** … PWA としてインストールでき、通信なしで制作できます
- **無段階の音色変化** … 音程・減衰・アタック・歪みを実時間で連続的に変えられます
  （サンプル方式では収録時の音色から離れられません）

書き出した WAV / MIDI の権利はユーザーに帰属し、商用利用も自由です。

## 音源のしくみ

`src/drums/audio/drum-processor.js`（AudioWorklet）に、方式の異なる9種類のボイスが入っています。
すべて 1 サンプルずつ計算しており、フィルターは TPT 型ステートバリアブルフィルター
（ローパス／バンドパス／ハイパスを同時に取り出せる形式）です。

| 音 | 合成方式 |
| --- | --- |
| キック | 正弦波 + 指数ピッチエンベロープ（打った瞬間だけ高い）+ クリックノイズ + tanh サチュレーション |
| スネア | 2本の正弦波（基音と 1.588 倍）による胴 + バンドパスノイズの響き線。2つは別々に減衰 |
| クラップ | バンドパスノイズを 4〜5 回の短いバーストで叩き、最後だけ長い余韻を残す |
| リムショット | 1720 Hz と 468 Hz の減衰正弦波 + 極短ノイズをバンドパスに通す |
| ハイハット | 非整数比（1 : 1.483 : 1.801 : 2.547 : 2.632 : 3.897）の矩形波6基 → ハイパス／バンドパス |
| シンバル | 同じ金属バンク + ノイズ。ライドは 3.35 kHz の共振「ピン」を追加、クラッシュは立ち上がりを遅く |
| タム | 正弦波 + 緩やかなピッチ落ち + 打面ノイズ |
| カウベル | 540 Hz / 800 Hz の矩形波をバンドパスに通す |
| シェイカー | 高域バンドパスノイズ。立ち上がりに時間をかけて「振る」感触を出す |
| パーカッション | 正弦波 + 倍音 + スラップノイズ（コンガ／ボンゴ的） |

各トラックは **音程・減衰・トーン・アタック・ドライブ・音量・定位・リバーブ送り・ディレイ送り**
の9パラメータを持ちます。トーンとアタックの意味は方式ごとに異なります（画面にも説明が出ます）。

| 方式 | トーン | アタック |
| --- | --- | --- |
| キック | アタックのピッチの高さ | ビーターのクリック音 |
| スネア | ノイズの明るさ | スナッピー（響き線）の量 |
| クラップ | 手のあたる高さ | 手の重なり具合（バースト数） |
| ハイハット／シンバル | 金物の明るさ | ノイズ成分の量 |
| タム | 打面の張り | 打面のノイズ |
| シェイカー | 粒の細かさ | 立ち上がりの速さ |

ハイハットは**チョークグループ**で結ばれており、クローズドを叩くとオープンが実機同様に止まります。

## シーケンサー

シーケンサーも音源と同じ AudioWorklet の中で動いています。`setTimeout` や `requestAnimationFrame`
に頼らず**サンプル単位**で発音位置を決めているため、テンポが揺れません。実測でも
120 BPM の16分が 0.125 秒ちょうど、スウィング 66% が 0.165 秒、8分3連が 0.16667 秒に並びます。

- **8パターン（A〜H）** … 各パターンは 1〜64 ステップ
- **14トラック** … キック / スネア / クラップ / リムショット / クローズドハット / オープンハット /
  ロー・ミドル・ハイタム / ライド / クラッシュ / カウベル / シェイカー / パーカッション
- **ステップごとの設定** … 強さ（ゴースト／ノーマル／アクセント、または 5〜100 の連続値）、
  **発音確率**、**連打（ロール 1〜6）**、**位置のずらし（±50%）**
- **ポリメーター** … トラックごとに独立した長さを設定できます（例：シェイカーだけ 7 ステップ）
- **スウィング 50〜75%** と **ヒューマナイズ**（タイミングと強さの微妙な揺らぎ）
- **細かさ** … 16分 / 8分3連 / 16分3連
- **ソングモード** … パターンと繰り返し回数を並べて 1 曲にします

## 機能

- **打ち込み** … マス目をタップ／ドラッグ、長押し（右クリック）でステップの詳細。元に戻す（Ctrl+Z）、
  左右にずらす、倍に伸ばす、パターンの複製・消去
- **パッド演奏** … 14パッド（押す位置で強弱が変わります）、PCキーボード、
  **REC** を点灯させて再生すると叩いた音がステップに自動で書き込まれます
- **キット8種** … クラシック・アナログ / パンチ・マシン / アコースティック / ローファイ・ヒップホップ /
  テクノ・インダストリアル / ハウス / トラップ / アンビエント
- **ミキサー** … トラックごとの音量・定位・2系統のセンド・ミュート・ソロ、トラック別レベルメーター
- **マスター** … サチュレーション、低域／高域シェルフ、バスコンプ（グルー）、ソフトクリップのリミッター
- **空間系** … その場生成のインパルス応答によるリバーブ4種（ルーム / プレート / ホール / カヴァーン）、
  テンポ同期ディレイ（1/16・3連・1/8・付点・1/4、ピンポン可）
- **書き出し**
  - **WAV**（48kHz / 24bit ステレオ）… 画面で聴いたものと同じ演奏をオフライン合成
  - **トラック別 WAV（ZIP）** … 14本のステムを 1 つの ZIP にまとめて書き出し（他のDAWで混ぜ直せます）
  - **標準MIDIファイル** … GM ドラムマップ（チャンネル10）。スウィング・ずらし・ロールも反映
  - **プロジェクト（JSON）** … パターン・音色・ソングをまとめて保存／読み込み
- **デモ10曲**（下記）と **PWA**（ホーム画面に追加してオフライン起動、内容は端末内に自動保存）

## 収録デモ（10曲）

| 曲名 | テンポ | キット | 内容 |
| --- | --- | --- | --- |
| ディープ・ハウス | 124 | ハウス | 四つ打ち + 裏拍のオープンハット |
| ハード・テクノ | 138 | テクノ | 16分ハットとロールで押し切る |
| ブーンバップ | 88 | ローファイ | スウィング 58%、ゴーストノート中心 |
| トラップ | 140 | トラップ | ハーフタイム + ハイハットロール |
| ドラムンベース | 174 | パンチ | 2小節のブレイクビート |
| エイトビート | 128 | アコースティック | ロックの基本形 |
| ファンク・シャッフル | 104 | アコースティック | スウィング 62% の強いハネ |
| ラテン・パーカッション | 100 | アコースティック | クラーベを軸にした構成 |
| ポリメーター実験 | 96 | アンビエント | シェイカー7歩・パーカッション5歩 |
| シンセポップ | 118 | クラシック・アナログ | 80年代風のカウベルとクラップ |

デモは `src/drums/data/songs.ts` に文字譜として書かれており、音声ファイルは含みません。

```ts
// 4ステップごとに `|` で区切る（`|` は読み飛ばされる）
kick:  'X...|X...|X...|X...',   // X アクセント / x 通常 / o ゴースト / . 休み
ch:    'oxox|oxox|oxox|oxRx',   // r 2連打 / R 3連打 / ? 50%の確率
```

### デモを作るときの基準（重要）

収録できるのは **本アプリのために書き下ろしたリズムパターン**と、
**著作権の保護期間が満了した楽曲**だけです。保護期間中の楽曲のドラムパターンを
そのまま同梱・配信すると、複製権・公衆送信権の対象となり、JASRAC 等の管理団体からの
利用許諾（有償）が必要になります。「完全無料で配布・販売できる」という本アプリの前提と
両立しないため、既存曲の再現データは収録していません。

なお、**キット名に実在の機種名（TR-808 等）は使っていません**。機種名は各社の商標であり、
商標権者以外が製品名として使うと出所の誤認を招くおそれがあるためです。本アプリでは
「クラシック・アナログ」「パンチ・マシン」のように音のキャラクターで名付けています。

## 操作方法

| 操作 | 割り当て |
| --- | --- |
| 打ち込み | マス目をクリック／タップ（マウスはドラッグで連続入力） |
| ステップの詳細（強さ・確率・連打・ずらし） | マス目を長押し、または右クリック |
| 再生 / 停止 | Space または ▶ ボタン |
| 録音（パッド演奏の書き込み） | R または REC ボタン |
| パターン切替 | 数字キー 1〜8、または A〜H ボタン |
| パッド演奏 | `A S D F G H J`（上段7音）/ `Z X C V B N M`（下段7音）、Shift で強打 |
| 元に戻す | Ctrl+Z（Mac は ⌘Z） |
| 全停止 | Esc または画面右上の ■ ボタン |

## ディレクトリ構成

```
drums/index.html                    アプリのHTMLエントリー（/drums/ で公開）
src/drums/
  main.ts                           起動・Service Worker 登録
  audio/
    drum-processor.js               AudioWorklet：音源9方式 + シーケンサー本体
    DrumEngine.ts                   ミックスバス（サチュレーション・EQ・コンプ・リバーブ・ディレイ）
    kits.ts                         14トラックの定義とキット8種
    project.ts                      プロジェクトの生成・保存形式・文字譜の解釈
    export.ts                       WAV / 標準MIDI / ZIP（無圧縮）の書き出し
    reverb.ts                       インパルス応答の生成（音声ファイル不使用）
    types.ts                        パラメータ・パターン・ソングの型
  ui/
    App.ts                          画面全体の構成と配線
    StepGrid.ts                     ステップグリッド（打ち込み・再生位置・メーター）
    Pads.ts                         パッド演奏
    controls.ts                     スライダー等のUI部品
  data/songs.ts                     デモ10曲
  styles/drums.css                  UIテーマ
public/drums/                       manifest / Service Worker / アイコン / プライバシーポリシー
scripts/gen-drum-icons.mjs          アイコンPNGの生成（外部ライブラリ不使用）
electron/drums-main.cjs             Windows デスクトップ版のエントリーポイント
```

## 公開・配布（すべて無料の手段）

```bash
npm run build            # dist/drums/ に静的ファイルを出力
npm run preview          # http://localhost:4173/drums/ で確認
```

- **Web / PWA** … `main` ブランチへの push で GitHub Pages に自動デプロイされ、
  `https://<公開URL>/drums/` で開けます（`.github/workflows/deploy-pages.yml`）。
  スマホでは「ホーム画面に追加」でアプリとして起動できます。
- **Windows デスクトップ版** … `npm run drums:build:win` でインストール不要の
  ポータブル exe を生成します（Electron / electron-builder、いずれも無料の OSS）。
- **アイコンの再生成** … `npm run drums:icons`

## 販売する場合

音源・パターン・アイコン・残響のすべてが自作物なので、そのまま有料販売できます。
（ロイヤリティの支払先も、再配布が禁止された素材もありません。）

- **ストア配布** … Windows 版はポータブル exe をそのまま販売できます。Microsoft Store や
  Google Play（PWA を TWA として包む）に出す場合は、各ストアの登録料が別途かかります。
- **表示すべきもの** … プライバシーポリシー（`public/drums/privacy.html` に同梱）と連絡先。
- **注意** … 販売時も「TR-808 互換」等の実在機種名を売り文句にしないでください（商標）。
  「アナログ方式のドラムシンセ」のように機能で説明すれば問題ありません。

## 動作環境

AudioWorklet に対応したブラウザが必要です（Chrome / Edge / Firefox / Safari 14.1 以降）。
14トラック同時発音でも CPU 負荷は低く、スマートフォンでも動作します。

## ライセンスと権利について

- 音源・残響・アイコン・デモパターンを含め、**第三者の著作物を一切含みません**。
- 本アプリで書き出した音源の利用に制限はありません（商用利用可・クレジット表記不要）。
