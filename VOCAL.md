# Hoshizora Vocal 🎤

**完全無料**・広告なし・アカウント不要の、日本語**歌声合成（ボーカル）アプリ**です。
人の声を録音した音源（サンプル／ボイスバンク）を一切使わず、**声帯の振動と声道の共鳴を
その場で計算**して歌わせています。ピアノロールに音符を置き、かなで歌詞を書くだけで
日本語で歌い、伴奏付きのミックスを **WAV / MIDI** に書き出せます。

このリポジトリのシンセ（Micro Sakura Studio）・ピアノ（Aozora Grand Piano）とは
独立したアプリで、`/vocal/` で公開されます。

- 開発サーバー … `npm run vocal:dev` → <http://localhost:5174/vocal/>
- 本番ビルド … `npm run build` → `dist/vocal/`

---

## なぜ「収録音声を使わない」のか

一般的な歌声合成ソフトは、歌手を収録した音声素材（ボイスバンク）を切り貼りして歌わせます。
この方式は品質が高い一方で、**収録音源のライセンス**と**歌手の権利**がつきまといます。
本アプリは声そのものを合成しているため、

- **ライセンス費用ゼロ** … 第三者の録音物・IR・音声ライブラリを一切含みません
- **配信コストゼロ** … アプリ全体で約 30 KB（gzip 圧縮時）。追加ダウンロードなし
- **オフライン動作** … PWA としてインストールでき、通信なしで作曲できます
- **声を連続的に変えられる** … 声道の長さ・張り・息の量などを実時間で無段階に変更できます
  （収録方式では、録った声から別人の声にはできません）
- **権利がはっきりしている** … 実在の人物の声を模したものではなく、すべて数式上の架空の声です

**書き出した音源は、配信・販売を含めて自由に使えます**（許諾・使用料・クレジット表記は不要）。
歌詞やメロディに第三者の著作物を使う場合のみ、その権利者の許諾が別途必要です。

## 歌声のしくみ

`src/vocal/audio/vocal-processor.js`（AudioWorklet）で動く**ソース・フィルタ型**の
歌声合成エンジンです。人の発声と同じ構造をそのまま計算しています。

| 要素 | モデル |
| --- | --- |
| 声帯（音源） | 声門流の微分波。開放相を多項式で描き、閉じる瞬間に**戻り相**を置いて鋭さを決める |
| 開放率 / 戻り相 | 声の張りと音量から算出。張るほど鋭く（明るく）、緩めるほど柔らかくなる |
| ゆらぎ | 周期ごとの**ジッタ**（音程）と**シマー**（音量）+ ゆっくりしたピッチドリフト |
| 声道 | **5段の共振器カスケード**（F1〜F5）。母音ごとにフォルマント周波数と帯域幅を持つ |
| 鼻音 | 極零対（反共振 + 共振）。「ん」「ま」「な」や、鼻にかかった声を作る |
| 気息 | 声門が開いている間に強く出る、声道を通る雑音（息漏れ・ウィスパー） |
| 摩擦・破裂 | 声道とは別経路のバンドパス2段。さ行の高い擦過音や破裂音の帯域を再現 |
| 声の高さ感 | 声道スケール。F1 は顎の開き寄り、F2 以上は声道長に比例して動かす |
| 高音の処理 | 基音がF1を超えたらF1を基音まで持ち上げる（実際の歌手と同じ挙動） |

音素の定義は `phonemes.ts`（母音6・子音23）にまとめ、後続母音に合わせて破裂音の帯域や
ロクスを変える**調音結合**も入れています（「か」と「き」で k の音が変わります）。

## 歌わせ方のしくみ

`src/vocal/audio/compile.ts` が楽譜を「パラメータごとの折れ線」に変換します。
折れ線にしておくことで、**再生と WAV 書き出しが完全に同じ波形**になります。

- **子音は拍の手前に置く** … 母音が拍の頭に来るように歌う（実際の歌唱と同じ）
- **音程をつなぐ** … ポルタメント、フレーズ頭の**しゃくり**、音符ごとのビブラート
  （深さ・速さ・かかり始めを指定でき、長い音符にだけ自然にかかります）
- **語尾の処理** … 撥音「ん」への移行、促音「っ」での切り、息が抜けるリリース
- **息継ぎ** … フレーズの前にブレス音を入れる（音符ごとに指定可）
- **強弱** … 音符ごとのベロシティが声の張り・明るさ・息の量まで変えます

## 機能

- **ボイス 8種** … 宵（女性ポップ）/ 澪（ソプラノ）/ 燈（キュート）/ 凪（ウィスパー）/
  蒼（テノール）/ 陸（ハスキー）/ 響（バリトン）/ 小鳥（子ども）
- **歌詞入力** … ひらがな・カタカナ・ローマ字。拗音「きゃ」、促音「っ」、撥音「ん」、
  長音「ー」、外来音「ふぁ・てぃ・じぇ」まで対応。歌詞をまとめて貼り付けて
  **先頭の音符から自動で割り当て**られます（空白は息継ぎの位置になります）
- **ピアノロール** … マウスでもタッチでも、追加・移動・長さ変更・範囲選択・ダブルクリックで
  歌詞入力。Tab で次の音符へ連続入力。ピンチズーム、取り消し / やり直し対応
- **伴奏** … コード進行（`Am7` `F#m7b5` `G/B` などに対応）から
  バラード / ポップ / アルペジオ / パッド / バンドの5スタイルを自動生成。
  伴奏の音（エレピ・パッド・ベース・撥弦・ドラム）もすべて合成音源です
- **ミックス** … ハイパス・EQ・コンプ・ダブラー・ステレオ幅・ディレイ（拍同期）・
  リバーブ（ルーム / プレート / ホール / 大聖堂、インパルス応答もその場生成）
- **書き出し** … **WAV（48kHz / 24bit ステレオ、ピーク自動そろえ）** を
  ミックス / ボーカルのみ / 伴奏のみ の**ステム**で出力。
  **歌詞メタイベント付きの標準MIDI**、プロジェクト（JSON）の保存・読込も可能
- **デモ曲 6曲** … すべて本アプリのための書き下ろし（詞・曲ともオリジナル）

## 使い方

1. 「▶ 再生」でデモ曲を聴く（最初のクリックでオーディオが起動します）
2. 「曲」タブの**歌詞のまとめ入力**に歌詞を貼り、「先頭から流し込む」
3. ピアノロールで音符を置き、ダブルクリック（または Enter）で歌詞を直す
4. 「声」「歌い方」タブで声色と歌い方を調整
5. 「伴奏」タブにコードを1行1小節で書く
6. 「書き出し」から WAV / MIDI を保存

### ショートカット

| キー | 動作 |
| --- | --- |
| Space | 再生 / 停止 |
| Enter | 選択した音符の歌詞を編集 |
| Tab（歌詞入力中） | 次の音符へ |
| ← → | 音符を左右に移動（スナップ単位） |
| ↑ ↓ | 音程を半音移動（Shift でオクターブ） |
| Delete / Backspace | 選択した音符を削除 |
| Ctrl + Z / Ctrl + Shift + Z | 取り消し / やり直し |
| Ctrl + A | すべて選択 |

## ディレクトリ構成

```
src/vocal/
  audio/
    vocal-processor.js  歌声合成エンジン（AudioWorklet。伴奏音源も同居）
    compile.ts          楽譜 → 制御曲線（子音配置・音程・ビブラート・強弱）
    phonemes.ts         母音のフォルマントと子音の定義（調音結合つき）
    kana.ts             かな／ローマ字 → 音素
    voices.ts           ボイスプリセット8種
    chords.ts           コード解釈と伴奏パターン生成
    VocalEngine.ts      信号経路（EQ / コンプ / ダブラー / ディレイ / リバーブ）と書き出し
    reverb.ts           インパルス応答の生成
    export.ts           WAV / MIDI / プロジェクトの書き出し
    song.ts             曲データの操作（歌詞流し込み・移調など）
    types.ts            データモデル
  ui/
    App.ts              画面全体の組み立て
    PianoRoll.ts        歌詞つきピアノロール（canvas）
    controls.ts         UI 部品
  data/demos.ts         デモ曲（オリジナル）
  styles/vocal.css      UIテーマ
public/vocal/           manifest / Service Worker / アイコン / プライバシーポリシー
scripts/gen-vocal-icons.mjs  アイコンPNGの生成（外部ライブラリ不使用）
electron/vocal-main.cjs      Windows デスクトップ版のエントリーポイント
```

## 公開・配布（すべて無料の手段）

```bash
npm run build            # dist/vocal/ に静的ファイルを出力
npm run preview          # http://localhost:4173/vocal/ で確認
```

- **Web / PWA** … `main` ブランチへの push で GitHub Pages に自動デプロイされ、
  `https://<公開URL>/vocal/` で開けます（`.github/workflows/deploy-pages.yml`）。
  スマホでは「ホーム画面に追加」でアプリとして起動できます。
- **Windows デスクトップ版** … `npm run vocal:build:win` でインストール不要の
  ポータブル exe を生成します（Electron / electron-builder、いずれも無料の OSS）。
- **アイコンの再生成** … `npm run vocal:icons`

## 動作環境

AudioWorklet に対応したブラウザが必要です（Chrome / Edge / Firefox / Safari 14.1 以降）。
1曲まるごとの WAV 書き出しはオフラインレンダリングのため、実時間より速く終わります。

## ライセンスと権利について

- 音源・残響・アイコン・デモ楽曲を含め、**第三者の著作物を一切含みません**。
- 8種類の声はいずれも実在の人物を模したものではなく、パラメータから作った架空の声です。
- デモ曲6曲は本アプリのための書き下ろしで、詞・曲とも本リポジトリのオリジナルです。
- 本アプリで書き出した音源の利用に制限はありません（**商用利用可**・クレジット表記不要）。
