# Aozora Grand Piano 🎹

**完全無料**・広告なし・アカウント不要の、物理モデリング方式グランドピアノ・アプリです。
録音済みのピアノ音源（サンプル）を一切使わず、**88鍵それぞれの弦の振動をその場で計算**して
音を作っています。アプリ本体は約 60 KB（gzip 圧縮時）で、追加ダウンロードは一切ありません。

このリポジトリのシンセ（Akatsuki Synth）とは独立したアプリで、`/piano/` で公開されます。

- 開発サーバー … `npm run piano:dev` → <http://localhost:5174/piano/>
- 本番ビルド … `npm run build` → `dist/piano/`

---

## なぜ「サンプルを使わない」のか

一般的なピアノアプリは、実際のグランドピアノを録音した音源（数百 MB〜数 GB）を再生しています。
高音質なサンプル音源は**有償ライセンス**であることが多く、無料配布や商用販売の障害になります。
本アプリは音を物理モデルで合成しているため、

- **ライセンス費用ゼロ** … 第三者の録音物・IR・ライブラリを一切含みません
- **配信コストゼロ** … 数百 KB の静的ファイルのみ。CDN 費用も課金も不要
- **オフライン動作** … PWA としてインストールでき、通信なしで演奏できます
- **無段階の音色変化** … ハンマーの硬さや打弦位置を実時間で連続的に変えられます
  （サンプル方式では収録時の音色から変えられません）

書き出した WAV / MIDI の権利はユーザーに帰属し、商用利用も自由です。

## 音源のしくみ

`src/piano/audio/piano-processor.js`（AudioWorklet）で動く**モーダル合成**エンジンです。

| 要素 | モデル |
| --- | --- |
| 弦 | 部分音ごとの2極共振器バンク（1音あたり最大56モード） |
| 部分音の周波数 | `fk = k·f0·√(1 + B·k²)`。弦の硬さによる**不協和度 B** を音域ごとに設定 |
| ハンマー | 2つの指数関数の差でできた力パルス。**打弦時間**が短いほど倍音が増える |
| ベロシティ | 強打ほど打弦時間が短くなり、音量だけでなく**音色が明るく**変化 |
| 打弦位置 | `|sin(kπα)|` によるモード振幅。端に近いほど硬い音 |
| ユニゾン弦 | 低次部分音を2本のわずかにずれた弦に分割し、**うなり**を生成 |
| 減衰 | 部分音ごとに時定数を変え、高次倍音ほど早く減衰（低音は数十秒、高音は数秒） |
| ダンパー | 離鍵時に音域依存の速度で減衰。実機同様、**最高音域にはダンパーなし** |
| 共鳴 | サステインペダル時に働く共鳴弦バンク（12音 × 4部分音） |
| 調律 | 基準ピッチ可変（415〜448 Hz）+ **ストレッチ調律**（レイルズバック曲線） |
| 響き | 大屋根の開き（フィルター）・響板EQ・その場生成のインパルス応答による残響 |

音量は部分音ごとの伝達量から自動正規化しているため、A0 から C8 まで音量差が揃います。

## 機能

- **演奏** … 88鍵の画面鍵盤（マルチタッチ / グリッサンド対応、打鍵位置でベロシティが変化）、
  PCキーボード、**Web MIDI**（CC64 サステイン / CC66 ソステヌート / CC67 ソフトに対応）
- **3本ペダル** … サステイン（Space）、ソステヌート、ソフト（Shift）
- **音色プリセット 8種** … コンサートグランド / スタジオグランド / ウォームバラード /
  ブライトポップ / ジャズクラブ / フェルトピアノ / シネマティック / ホンキートンク
- **音づくり** … ハンマーの硬さ・打弦位置・減衰・弦の共鳴・ユニゾンのうなり・打弦ノイズ・離鍵ノイズ
- **響き** … 大屋根の開き、トーン、残響（ルーム / スタジオ / ホール / 教会）
- **演奏設定** … ベロシティカーブ、ダイナミクス、基準ピッチ、ストレッチ調律、同時発音数、
  鍵盤サイズ・表示範囲・音名表示、メトロノーム
- **録音と書き出し** … 演奏をイベントとして記録し、**WAV（48kHz / 24bit ステレオ）** と
  **標準MIDIファイル**に書き出し。録音後に音色を変えて書き出し直すこともできます
- **デモ演奏 12曲** … 下記の一覧を参照（すべて権利処理が不要な楽曲）
- **PWA** … ホーム画面に追加してオフライン起動。設定は端末内に保存されます

## 収録デモ曲（12曲）

| 曲名 | 作曲 | 権利 |
| --- | --- | --- |
| 前奏曲 第1番 ハ長調 BWV 846 | J.S. バッハ | パブリックドメイン |
| エリーゼのために WoO 59 | L.v. ベートーヴェン | パブリックドメイン |
| ピアノソナタ第14番「月光」第1楽章 | L.v. ベートーヴェン | パブリックドメイン |
| カノン ニ長調 | J. パッヘルベル | パブリックドメイン |
| メヌエット ト長調 BWV Anh.114 | C. ペツォールト | パブリックドメイン |
| 歓喜の歌（交響曲第9番より） | L.v. ベートーヴェン | パブリックドメイン |
| きらきら星 | フランス民謡 | パブリックドメイン |
| アメイジング・グレイス | 賛美歌 | パブリックドメイン |
| グリーンスリーブス | イングランド民謡 | パブリックドメイン |
| 星降る夜のワルツ | オリジナル | 本アプリ書き下ろし |
| 夏の終わりのバラード | オリジナル | 本アプリ書き下ろし |
| 夜明けのノクターン | オリジナル | 本アプリ書き下ろし |

デモは `src/piano/data/demos.ts` に楽譜文字列として記述されており、音声ファイルは含みません。

```ts
// 例：音名:長さ（長さは unit の倍数）、"+" で和音、"r" で休符
take.seq(time, 'D5:1 G4:0.5 A4:0.5 B4:0.5 C5:0.5', quarter, 0.52);
take.hit(time, ['G2', 'D3', 'B3'], quarter, 0.4);
```

### デモ曲を選ぶ基準（重要）

収録できるのは **著作権の保護期間が満了した楽曲**（作曲者の没後70年経過。日本では
2018年の改正前に50年で満了した作品を含む）と、**本アプリのオリジナル曲**だけです。
保護期間中の楽曲は、旋律をMIDIデータとして同梱・配信するだけでも複製権・公衆送信権の
対象となり、JASRAC 等の管理団体からの利用許諾（有償）が必要になるため、「完全無料で
配布・販売できる」という本アプリの前提と両立しません。

## 操作方法

| 操作 | 割り当て |
| --- | --- |
| 演奏 | 画面の鍵盤をクリック / タッチ（手前を押すほど強い音） |
| PCキーボード | `Z S X D C V G B H N J M , L . ; /` と `Q 2 W 3 E R 5 T 6 Y 7 U I 9 O 0 P` |
| オクターブ切替 | ← / → |
| サステインペダル | Space（押している間） / 画面のボタン（固定） |
| ソフトペダル | Shift |
| 全停止（音・ペダル・再生を止める） | Esc または画面右上の ■ ボタン |

## ディレクトリ構成

```
piano/index.html                    アプリのHTMLエントリー（/piano/ で公開）
src/piano/
  main.ts                           起動・Service Worker 登録
  audio/
    piano-processor.js              AudioWorklet：物理モデリング音源本体
    PianoEngine.ts                  ノード接続（大屋根EQ・コンプ・残響）とオフライン書き出し
    presets.ts                      音色プリセット
    reverb.ts                       インパルス応答の生成（音声ファイル不使用）
    recorder.ts                     録音・再生・WAV / MIDI 書き出し
    midi.ts                         Web MIDI 入力・PCキー配列
    types.ts                        パラメータと演奏イベントの型
  ui/
    App.ts                          画面全体の構成と配線
    Keyboard.ts                     88鍵の鍵盤（ポインタ / マルチタッチ）
    StringView.ts                   響板と弦のビジュアライザー
    Metronome.ts                    メトロノーム
    controls.ts                     スライダー等のUI部品
  data/demos.ts                     デモ楽曲データ
  styles/piano.css                  UIテーマ
public/piano/                       manifest / Service Worker / アイコン / プライバシーポリシー
scripts/gen-piano-icons.mjs         アイコンPNGの生成（外部ライブラリ不使用）
electron/piano-main.cjs             Windows デスクトップ版のエントリーポイント
```

## 公開・配布（すべて無料の手段）

```bash
npm run build            # dist/piano/ に静的ファイルを出力
npm run preview          # http://localhost:4173/piano/ で確認
```

- **Web / PWA** … `main` ブランチへの push で GitHub Pages に自動デプロイされ、
  `https://<公開URL>/piano/` で開けます（`.github/workflows/deploy-pages.yml`）。
  スマホでは「ホーム画面に追加」でアプリとして起動できます。
- **Windows デスクトップ版** … `npm run piano:build:win` でインストール不要の
  ポータブル exe を生成します（Electron / electron-builder、いずれも無料の OSS）。
- **アイコンの再生成** … `npm run piano:icons`

## 動作環境

AudioWorklet に対応したブラウザが必要です（Chrome / Edge / Firefox / Safari 14.1 以降）。
MIDI 入力は Web MIDI 対応ブラウザ（Chrome / Edge）でのみ利用できます。

## ライセンスと権利について

- 音源・残響・アイコン・デモ楽曲を含め、**第三者の著作物を一切含みません**。
- デモ曲はパブリックドメイン作品（J.S. バッハ）と本アプリのオリジナル曲のみです。
- 本アプリで書き出した音源の利用に制限はありません（商用利用可・クレジット表記不要）。
