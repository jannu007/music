# Yamabiko Sampler（サンプラー）

自分で取り込んだ音・録った音を鍵盤に割り当てて演奏できるサンプラーです。
`/sampler/` で公開し、Android アプリとしても同じ中身を配布します。

## 何ができるか

- **音を鍵盤に割り当てる。** 1つの素材を鍵盤の範囲・強さの範囲に割り当てた
  ものを「ゾーン」と呼び、いくつでも重ねられます。強さで音色を切り替える
  （ベロシティレイヤー）、同じ音を順番に鳴らし分ける（ラウンドロビン）といった、
  多段サンプラーの基本はひととおり入っています。
- **素材を持ち込む。** 音声ファイルの取り込みと、マイクでの録音。
- **波形を編集する。** 鳴らす範囲、ループ点、逆再生。
- **音をつくる。** エンベロープ、フィルター（鍵盤追従・エンベロープ付き）、
  LFO、単音／和音、グライド、移調。
- **エフェクト。** 6アプリ共通の `shared/audio/fx.ts` の10種類
  （ひずみ・ビットクラッシャー・フィルター・コーラス・フランジャー・
  フェイザー・リングモジュレーター・トレモロ／オートパン・ディレイ・リバーブ）。
- **書き出す。** 演奏を記録して WAV（48kHz / 24bit ステレオ）と MIDI へ。
  楽器そのものも1つのファイルに保存できます。
- **収録デモ10曲。** 押すと音源ごと読み込んで再生します。録音済みの状態で
  入るので、そのまま WAV / MIDI に書き出せます。
- **パッド16枚。** 録音した演奏を1枚ずつ載せて、押すと鳴らせます。

## パッド

録音タブに 4×4 のパッドがあります。空いているパッドを押すと**いまの録音が載り**、
載ったパッドを押すと**鳴ります**。右上の × で空にできます。

載っているのは音符ではなく**音そのもの**です。載せるときに、エフェクトまで
含めて一度だけ焼いてしまいます。押した瞬間に鳴らないと楽器にならないからで、
そのつど合成し直していては数百ミリ秒かかって間に合いません。実機のサンプラーの
パッドと同じ考え方で、あとから音づくりを変えても、載せたときの音のまま残ります。

同じパッドを続けて押すと、前の音を止めてから鳴らし直します。重ねてしまうと
連打のたびに音が厚くなっていくためです。

パッドの中身は IndexedDB に 16bit で保存するので、開き直しても残ります。
1枚あたり30秒を上限にしています（際限なく載せると端末のメモリを食うため）。

段ごとに色が変わります。並びのどこを押しているかが、目を落とさなくても
分かるようにするためで、実機のパッドが段ごとに光り分けているのと同じ理由です。

## 画面の作り

タブは5つ（割り当て / 音づくり / エフェクト / 録音 / 書き出し）。
音源選びと収録デモは「割り当て」の中にあります。音を選ぶことと、それを鍵盤へ
割り当てることは続けて行う作業なので、行き来させない方が速いためです。

**波形は画面の下に出しっぱなし**にしてあります。どのタブを見ていても、
いま触っている音がどんな形をしているかが見えます。そのままドラッグして
鳴らす範囲も変えられます。

波形は、明るい本体の下に薄い「返り」を描いています。これはアイコンと同じ形で、
アプリの名前（山彦＝入れた音がそのまま返ってくる）そのものです。

## 収録デモについて

10曲とも本アプリのために書き下ろしたもので、第三者の楽曲は含みません。
鳴っているのは付属音源なので、**デモを書き出した音源もそのまま商用利用できます**。

記譜は `sampler/src/data/demos.ts` に文字列で置いてあります。

```
D3 . . . | . . A3 . | . . . . | F3 . . .
```

1トークンが1ステップ。`.` は休み、`-` は直前の音を伸ばす、`+` で和音、
`!` で強く、`,` で弱く。`|` は読みやすさのための区切りです。

文字列なので書き間違えても型では気づけません。`npm run sampler:test` の
デモ検査が、**書いた音の数と実際に鳴った音の数が一致するか**まで見ています
（読めなかったトークンがあれば、その音は黙って消えるため）。

## 付属音源について

**サンプラーなのに、録音素材をひとつも同梱していません。** 付属音源は
すべて起動時にその場で合成しています（`sampler/src/audio/factory.ts`）。

| 音源 | 作り方 |
| --- | --- |
| 木の弦 / 鋼の弦 | Karplus-Strong（弦の往復をディレイで模す） |
| 霞 / 空洞 | 倍音を重ね、非整数比のうなりを混ぜる |
| 鈴野 | 非整数比の正弦波を重ねて、音程の定まらない金物にする |
| 打楽器 | 帯域を絞った雑音と、急に落ちる正弦波 |

これは音づくりの好みではなく、**販売の条件をきれいにするため**です。
第三者の録音を同梱すると、その使用許諾がアプリの販売条件に縛りをかけます。
ここで鳴っている音はすべて計算から出ているので、付属音源も、それで作った音源も、
そのまま商用利用できます。

合成は決まった順番の疑似乱数から行うので、いつ作り直しても同じ音になります。
そのため付属音源は保存せず、選ばれたものだけをその都度合成します
（全部を抱えると端末のメモリを食うため。1音源あたり 1.1〜4.4 MB）。

## 再生のしくみ

ほかの5本と違い、AudioWorklet を使っていません。サンプラーの仕事は
「波形をなぞって、再生速度で音程を変える」ことで、これは
`AudioBufferSourceNode` がブラウザの中で最適化して行っています。
自分で書くより速く、音も良いので、そのまま使っています。

1音ぶんの流れ:

```
BufferSource → ゲイン（エンベロープ） → フィルター → パン ─┐
                                                          ├→ 楽器の出口 → エフェクト
LFO → 音程・遮断周波数・音量                               ┘
```

## セキュリティ

このアプリは**通信を一切しません**。それを作りの都合で終わらせず、
ブラウザ自身に守らせています。

### 通信を禁じる

`sampler/index.html` に Content-Security-Policy を置いています。

| 指定 | 意味 |
| --- | --- |
| `default-src 'none'` | 既定は全部禁止。必要なものだけ開ける |
| `connect-src 'none'` | fetch / XHR / WebSocket を一切許さない |
| `script-src 'self'` | 同梱したスクリプトだけ。インラインも `eval` も不可 |
| `object-src 'none'` | プラグインの埋め込みを禁止 |
| `base-uri 'none'` | `<base>` による参照先のすり替えを禁止 |
| `form-action 'none'` | どこへも送信させない |

うっかり外部への参照を書いてしまっても、ブラウザの側で止まります。
`npm run sampler:test` は、実際に `fetch('https://example.com/')` を試して
**止められること**まで確かめています。

`frame-ancestors` はここには書いていません。`<meta>` で配るとブラウザに
無視されるためです（HTTP ヘッダーでしか効かない）。代わりに `main.ts` で
「他サイトの枠の中にいないか」を見て、いるなら起動しません。マイクの許可を
求める操作があるので、無関係なページに埋め込ませないためです。

### 持ち込まれるものを信用しない

外の世界に触れるのは2か所だけで、どちらも「中身を書いたのはこちらではない」
という前提で書いてあります。

**音声ファイル**（`import.ts`）— 大きさ（64MB）と長さ（90秒）に上限を設け、
拡張子ではなく実際に音として読めるかどうかで判断します。形式の解析は
`decodeAudioData` に任せます。壊れたファイルや細工されたファイルの扱いは、
自前で書くよりブラウザに任せたほうが安全で確実だからです。

**楽器ファイル**（`project.ts`）— 読み込みは「既定値から始めて、認めた項目だけを、
範囲を確かめて上書きする」という書き方だけで通しています。知らない項目は捨てます。
`__proto__` や `constructor` は必ず外します。JSON の中身をそのまま組み立てに使うと、
他のオブジェクトのふるまいまで書き換えられてしまうためです（プロトタイプ汚染）。

これは `npm run sampler:test` の後半で、わざと壊れたもの・悪意のあるものを
渡して確かめています（範囲外の値、逆さまの範囲、`__proto__` を含む JSON、
base64 でない波形、`../../etc/passwd` のような id）。

### マイクと保存データ

マイクの許可を求めるのは、押されたときだけです。録り終わると
`track.stop()` でただちに解放します（残すと端末に録音中の表示が残り続けます）。

取り込んだ音は IndexedDB に 16bit で保存します。保管庫は 256MB を上限にして、
超えたら使っていないものから消します。アプリ内の「取り込んだ素材をすべて消す」で
いつでも空にできます。

## 検証

```bash
npm run build
npm run sampler:test
```

1つめはヘッドレスの Chromium で実際に鳴らし、起動・発音・各タブ・波形描画・
収録デモの読み込み・録音・WAV 書き出しの中身（無音でないこと、歪んでいないこと）・
通信ゼロ・CSP を確かめます。2つめはブラウザを立てずに、壊れたファイルを
検証関数へ直接渡します。3つめは収録デモ10曲を1曲ずつオフラインで書き出し、
無音・歪み・NaN・途中の途切れが無いかを測ります。

なお **Playwright の `waitForFunction` はこのページでは使えません**。内部で
`eval` を使うため CSP に弾かれます。テスト側は `evaluate` を繰り返す形で
待っています（CSP が効いている証拠でもあります）。
